# Chave da API Gemini (opcional para este checklist)
GEMINI_API_KEY=

# Make.com — envio de ações do checklist para o cenário Make
# Obtenha a URL em: Make > Cenário > Webhooks > "Custom webhook" (envio)
VITE_MAKE_WEBHOOK_URL=https://hook.us2.make.com/xxxxxxxxxxxxx

# Para receber o retorno do Make: no cenário Use "Respond to a webhook" ou
# "HTTP - Make a request" apontando para a URL do seu servidor de webhook:
#   Desenvolvimento (com npm run server): use ngrok ou similar para expor
#   http://localhost:3001/api/webhooks/make-callback
#   Produção: use a URL pública do seu backend + /api/webhooks/make-callback
